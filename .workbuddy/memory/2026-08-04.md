# 2026-08-04 工作日志

## 破晓对齐挟剑功能（app.js 17处硬编码修复）
- server.js / multiplayerSync.js 大部分修改已在之前完成
- app.js 修复：
  - _initMultiplayer: roomState handler 添加 poxiao 标志，入口浮层 if-else 扩展，10s 超时回退
  - 共享函数 7 处：_refreshXiejianRemotePlayers, _updateXiejianWorldItemStatus, _renderXiejianInventory, _renderXiejianItemDetail, _updateOnlinePlayersList, _startXiejianPromptLoop, _handleRemoteChat
  - checkAndInitGameMap 7 处：双人模式排除、showCurrentCharInfo、switch-char-btn、setCategory、首次渲染器初始化、地图名查表、tile 索引
- 版本号 v=20260804r → v=20260804s

## 破晓资源路径修复
- 问题：所有 poxiao 资源路径为 `poxiao/...` 但实际文件在 `sendbox/src/assets/poxiao/...`，导致 404
- 修复：创建目录联接 `D:\prj\mail\poxiao` → `D:\prj\mail\sendbox\src\assets\poxiao`
- 验证通过：地图、角色头像 HTTP 200

## 破晓角色动画增强
- 问题：破晓角色每动作仅 1 帧（frameCount:1），vs 挟剑 4 帧循环，导致死板
- 修复（gameMapRenderer.js）：
  - 空闲呼吸：缩放振荡 ±2.2% + 浮动 ±3px
  - 跑步弹跳：垂直弹跳 ±5px 模拟步频
  - 动作切换：180ms 缩放脉冲过渡
  - 搭档同步：错相位呼吸（偏移 0.7π）
- 版本号 v=20260804s → v=20260804t
